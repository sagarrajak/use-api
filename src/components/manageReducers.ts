import { ToolkitStore } from "@reduxjs/toolkit/dist/configureStore"

export function manageReducers(params: {
    store: ToolkitStore,
    reduxKey: string,
    reducer: string
}) {
 
}

export default manageReducers
